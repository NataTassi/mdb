{
  "name": "downloads",
  "private": true,
  "scripts": {
    "start:dev": "npx nodemon -L",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript" : "latest",
    "@types/node" : "latest",
    "ts-node": "latest",
    "nodemon": "latest",
    "rimraf": "latest"
  }
}
